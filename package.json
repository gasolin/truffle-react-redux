{
  "name": "truffle-react-redux",
  "version": "0.2.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "chain": "cd contracts && npm run chain",
    "compile": "cd contracts && npm run build",
    "migrate": "cd contracts && npm run deploy",
    "start": "cd web && npm start",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
